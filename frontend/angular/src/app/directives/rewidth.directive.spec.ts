import { RewidthDirective } from './rewidth.directive';

describe('RewidthDirective', () => {
  it('should create an instance', () => {
    const directive = new RewidthDirective();
    expect(directive).toBeTruthy();
  });
});
