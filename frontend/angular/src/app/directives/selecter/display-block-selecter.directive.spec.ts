import { DisplayBlockSelecterDirective } from './display-block-selecter.directive';

describe('DisplayBlockSelecterDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayBlockSelecterDirective();
    expect(directive).toBeTruthy();
  });
});
