import { DisplaySelecterItemDirective } from './display-selecter-item.directive';

describe('DisplaySelecterItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplaySelecterItemDirective();
    expect(directive).toBeTruthy();
  });
});
