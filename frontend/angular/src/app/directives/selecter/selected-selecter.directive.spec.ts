import { SelectedSelecterDirective } from './selected-selecter.directive';

describe('SelectedSelecterDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedSelecterDirective();
    expect(directive).toBeTruthy();
  });
});
