import { ChangeScaleDirective } from './change-scale.directive';

describe('ChangeScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeScaleDirective();
    expect(directive).toBeTruthy();
  });
});
