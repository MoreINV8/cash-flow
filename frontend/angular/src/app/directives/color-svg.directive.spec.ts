import { ColorSvgDirective } from './color-svg.directive';

describe('ColorSvgDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorSvgDirective();
    expect(directive).toBeTruthy();
  });
});
